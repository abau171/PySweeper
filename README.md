# PySweeper

Minesweeper written in Python 3 using tkinter.

## Running

Clone the repository and run `python3 main.py` to launch a new game.

## Customizing

You can edit the width and height of the mine grid, and the number of mines in `main.py`.  Simply update the `WIDTH`, `HEIGHT`, and `NUM_MINES` variables, respectively.
